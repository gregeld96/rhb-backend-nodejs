<div class="container-fluid">
    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-header">
                    <legend>{{ legend }}</legend>
                </div>
                <!-- /.card-header -->
                <div class="card-body">
                    {{#if messages.msg_info}}
                    <div class="alert alert-success alert-dismissible">
                        <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
                        <h5><i class="icon fas fa-check"></i> Success!</h5>
                        {{messages.msg_info}}
                    </div>
                    {{/if}}
                    {{#if messages.msg_error}}
                    <div class="alert alert-danger alert-dismissible">
                        <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
                        <h5><i class="icon fas fa-ban"></i> Alert!</h5>
                        {{messages.msg_error}}
                    </div>
                    {{/if}}
                    <form action="/cms/dynamic-pages/crisis-center/edit/{{data.id}}" method="post" class="mb-3"
                        enctype="multipart/form-data">
                        <div class="form-group">
                            <label for="criscen-title">Name</label>
                            <input type="text" class="form-control w-50" id="criscen-title" name="name"
                                placeholder="Rasyhid Doe" value="{{data.name}}">
                        </div>
                        <div class="form-group mb-4">
                            <label for="criscen-phone">Phone Number</label>
                            <input type="text" class="form-control w-50" id="criscen-phone" name="phone_number"
                                placeholder="0123456789" value="{{data.phone_number}}">
                        </div>
                        <div class="form-group">
                            <label for="thumbnail">Profile</label><br />
                            <img src="/photos/public/crisis_center/{{data.profile_pic}}" alt="logo"
                                style="max-width: 200px; max-height: 200px;"> <br /> <br />
                            <div class="input-group w-50">
                                <input type="file" id="profile_pic" name="criscen_pic" accept="image/*"
                                    placeholder="photo">
                            </div>
                            <p class="text-muted">Gambar dianjurkan berukuran 299x333 pixels</p>
                        </div>
                        <div>
                            <button id="create-btn" type="submit" class="my-auto btn btn-primary mr-3 mb-3"><i
                                    class="fas fa-check mr-1"></i>Update</button>
                            <a class="btn btn-success" href="/cms/dynamic-pages/crisis-center/">
                                <i class="fas fa-times"></i> Cancel
                            </a>
                        </div>
                    </form>
                </div>
            </div>
            <!-- /.card-body -->
        </div>
        <!-- /.card -->
    </div>
    <!-- /.col -->
</div>
<!-- /.row -->
</div>
<!-- /.container-fluid -->

<script>
    {{!-- var _URL = window.URL || window.webkitURL;
    var file, img, width, height;
    $("#profile_pic").change(function (e) {
        if ((file = this.files[0])) {
            img = new Image();
            img.onload = function () {
                width = this.width;
                height = this.height;
                $("#width").html(width);
                $("#height").html(height);
                if (width === 299 || height === 333) {
                    $("#create-btn").removeAttr("disabled")
                } else {
                    $("#create-btn").prop("disabled", true)
                    alert("Width max 299px and heigth max 333px");
                }
            };
            img.src = _URL.createObjectURL(file);
        } else {
            $("#create-btn").removeAttr("disabled")
        }
    }); --}}
</script>